services:

  sqlserver:
    build: .
    container_name: sqlserver

    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Sql1234@%"

    ports:
      - "1433:1433"

    volumes:
      - ./data:/var/opt/mssql

    restart: unless-stopped